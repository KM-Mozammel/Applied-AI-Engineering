# Determinant: Scalar value that indicates whether a matrix is invertible and how it scales space.
# Definition: Scalar showing scaling factor and invertibility.
# Analogy: Area scaling of a transformation.

import numpy as np
M = np.array([[1, 2], [3, 4]])

det = np.linalg.det(M)