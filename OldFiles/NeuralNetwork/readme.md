# A neuron receives multiple incoming signals. 
# Each signal is multiplied by the strength of its synaptic connection (weight), 
# then all these contributions are summed together. 
# Finally, the neuron adds its internal bias, which adjusts how easily it will fire.

# Modern AI adds:
activation functions
deep layers
learning algorithms
probability outputs